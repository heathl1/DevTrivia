package com.example.DevTrivia.auth;

import org.springframework.stereotype.Controller;

@Controller
public class AuthPagesController {
    // Intentionally left empty to avoid duplicate mappings.
    // AuthController already provides /login, /register, /reset, /guest.
}
